# 🚀 技师登录功能 - 快速启动指南

> 5 分钟快速测试技师登录功能

---

## ⚡ 快速启动 (Quick Start)

### 1️⃣ 启动后端 (30 秒)

```bash
cd backend

# 启动数据库
docker-compose up -d

# 运行迁移 (首次需要)
alembic upgrade head

# 创建测试账号 (首次需要)
python scripts/create_test_therapist.py

# 启动 API 服务
uvicorn app.main:app --reload
```

✅ 看到以下输出表示成功:
```
INFO:     Uvicorn running on http://0.0.0.0:8000
INFO:     Application startup complete.
```

---

### 2️⃣ 启动前端 (30 秒)

```bash
cd therapist

# 启动 Expo
npx expo start

# 按 'a' 在 Android 模拟器中运行
```

✅ 看到 Expo 二维码和命令提示表示成功

---

### 3️⃣ 测试登录 (1 分钟)

1. **App 自动打开到登录页**

2. **输入手机号**: `13800138000`

3. **点击"发送验证码"**
   - 按钮变灰，显示倒计时
   - 后端终端打印验证码

4. **输入验证码**: `888888` (万能码)

5. **点击"登录"**
   - 加载动画
   - 自动跳转到订单列表页

6. **✅ 登录成功！**

---

## 📱 测试账号

```
手机号: 13800138000
验证码: 888888 (开发环境万能验证码)
角色: 技师 (therapist)
```

---

## 🔍 验证登录成功

### 方法 1: 查看前端 Redux Store

打开 React Native Debugger:
```javascript
{
  auth: {
    isLoggedIn: true,
    token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    user: {
      id: "1",
      name: "测试技师",
      phone: "13800138000",
      role: "therapist"
    }
  }
}
```

### 方法 2: 查看后端日志

```
📤 API Request: POST /api/v1/therapist/auth/login
📥 API Response: 200 OK
{
  "access_token": "eyJ...",
  "therapist": {
    "name": "测试技师",
    "rating": 4.8,
    ...
  }
}
```

### 方法 3: 查看数据库

```bash
docker exec -it landa_postgres psql -U landa -d landa

SELECT id, phone, role, last_login_at FROM users WHERE phone = '13800138000';
```

输出:
```
 id |    phone     |   role    |      last_login_at      
----+--------------+-----------+-------------------------
  1 | 13800138000  | therapist | 2025-12-25 10:30:15
```

---

## ❓ 常见问题

### Q1: "该手机号未注册为技师"

**解决**:
```bash
cd backend
python scripts/create_test_therapist.py
```

### Q2: "Network Error"

**解决**:
- 检查后端是否启动: `curl http://localhost:8000/health`
- Android 模拟器使用 `10.0.2.2` 而不是 `localhost`
- 检查 `therapist/src/utils/constants.ts` 中的 `BASE_URL`

### Q3: 验证码未打印

**解决**:
- 检查 `backend/.env` 文件中 `DEBUG=true`
- 查看后端终端输出

### Q4: App 白屏或崩溃

**解决**:
```bash
# 清除缓存重启
cd therapist
npx expo start -c
```

---

## 🎯 核心特性

✅ **已实现**:
- ✅ 手机号验证码登录
- ✅ JWT Token 认证（包含 role）
- ✅ 角色权限验证
- ✅ 自动 Token 刷新（前端已支持）
- ✅ 登录状态持久化（Redux）
- ✅ 开发环境万能验证码

⏳ **待优化**:
- ⏳ Redis 存储验证码（当前内存存储）
- ⏳ 真实短信服务集成
- ⏳ Token 黑名单
- ⏳ 登录日志记录

---

## 📚 详细文档

- [完整实施总结](./IMPLEMENTATION_SUMMARY.md)
- [测试指南](./README_LOGIN_TEST.md)
- [API 文档](http://localhost:8000/docs) (启动后端后访问)

---

## 🎉 恭喜！

技师登录功能已成功实现并可以正常使用！

**下一步**:
1. 测试其他页面功能
2. 实现技师端业务 API
3. 完善前端交互细节

---

**需要帮助?** 查看完整文档或联系开发团队

