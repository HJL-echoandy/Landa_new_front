adb shell am force-stop com.andy_hwang.therapist; adb logcat -c; adb shell am start -n com.andy_hwang.therapist/.MainActivity



adb logcat -d *:S ReactNativeJS:V | Select-String "FCM Push Token" -Context 0,5 | Out-String


Invoke-WebRequest -Uri "http://192.168.13.145:8000/api/v1/therapist/notifications/debug/send-test-notification" -Method POST -ContentType "application/json" -Body '{"therapist_id": 3, "notification_type": "new_order"}' -UseBasicParsing | Select-Object -ExpandProperty Content


adb logcat -c; Start-Sleep -Seconds 5; adb logcat -d *:S ReactNativeJS:V | Select-String "FCM Push Token" -Context 0,5 | Out-String


adb reverse tcp:8081 tcp:8081


# 1. 重启应用
adb shell am force-stop com.andy_hwang.therapist; adb logcat -c; adb shell am start -n com.andy_hwang.therapist/.MainActivity

# 2. ADB 端口转发
adb reverse tcp:8081 tcp:8081

# 3. 查找占用端口的进程
netstat -ano | findstr :8081

# 4. 杀掉占用端口的进程
taskkill /F /PID <进程ID>

# 5. 启动 Metro bundler
cd D:\Landa_new_front\therapist
npx expo start --dev-client

# 6. 重新加载应用（在手机红屏时）
adb shell input text "RR"